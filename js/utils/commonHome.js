define(function(require) {
    return {
        $: require('jquery'),
        bs: require('bootstrap'),
        navWidget: require('utils/navWidget'),
        animate: require('utils/animate')
    };
});
